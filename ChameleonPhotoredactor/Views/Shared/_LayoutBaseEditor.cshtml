<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - Chameleon Editor</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="~/css/editor.css" />
    <link rel="stylesheet" href="~/css/guest.css" />


    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@40,400,0,0" />
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    <script src="~/js/site.js"></script>
</head>

<body>
    <div class="editor-layout">
        <nav class="editor-nav">

            <a asp-controller="Account" asp-action="Login"
               class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Account" &&
                                    ViewContext.RouteData.Values["action"]?.ToString() == "Login" ? "active" : "")">
                <div class="icon-container">
                    <span class="material-symbols-outlined" style="font-size: 24px;">arrow_back</span>
                </div>
                Back
            </a>

            <a asp-controller="Editor" asp-action="BaseEditor"
               class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Editor" &&
                                    ViewContext.RouteData.Values["action"]?.ToString() == "BaseEditor" ? "active" : "")">
                <div class="icon-container">
                    <span class="material-symbols-outlined discover_tune-rotate" style="font-size: 24px;">discover_tune</span>
                </div>
                Edit
            </a>

            <a asp-controller="Account" asp-action="Export"
               class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Account" &&
                                    ViewContext.RouteData.Values["action"]?.ToString() == "Export" ? "active" : "")">
                <div class="icon-container">
                    <span class="material-symbols-outlined" style="font-size: 24px;">download</span>
                </div>
                Export
            </a>



        </nav>

        <main class="guest-main">
            @RenderBody()
        </main>
    </div>

    <footer>
        <p>&copy; Chameleon 2025 - Your Photo Editor</p>
    </footer>
</body>
</html>